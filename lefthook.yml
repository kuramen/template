pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,vue}"
      run: pnpm lint:fix {staged_files}
      stage_fixed: true
    fmt:
      glob: "*.{ts,vue,css,json}"
      run: pnpm fmt {staged_files}
      stage_fixed: true

pre-push:
  commands:
    build:
      run: pnpm build
